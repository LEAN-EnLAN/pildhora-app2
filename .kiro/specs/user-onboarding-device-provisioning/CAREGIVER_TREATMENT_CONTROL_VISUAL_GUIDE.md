# Caregiver Treatment Control - Visual Guide

## Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     Caregiver Dashboard                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           Device Actions Panel                            │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  [Test Alarm]  [Dispense]  [Sync Time]                   │  │
│  │  [Check Status]  [Clear Alarm]                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           Critical Events Panel                           │  │
│  ├──────────────────────────────────────────────────────────┤  │
│  │  🔴 Dose Missed - John Doe                               │  │
│  │  📵 Device Offline - Jane Smith                          │  │
│  │  🔋 Low Battery - Bob Johnson                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

### Medication CRUD Operations

```
┌──────────────┐
│  Caregiver   │
│     App      │
└──────┬───────┘
       │
       │ 1. Create/Update/Delete Medication
       ▼
┌──────────────────┐
│ medicationsSlice │
│  (Redux Store)   │
└──────┬───────────┘
       │
       │ 2. Validate Permissions
       │    (caregiverId matches)
       ▼
┌──────────────────┐
│   Firestore      │
│  medications/    │
└──────┬───────────┘
       │
       │ 3. Generate Event
       ▼
┌──────────────────────┐
│ medicationEventService│
└──────┬────────────────┘
       │
       │ 4. Enqueue Event
       ▼
┌──────────────────┐
│   Firestore      │
│ medicationEvents/│
└──────────────────┘
```

### Device Actions Flow

```
┌──────────────┐
│  Caregiver   │
│     App      │
└──────┬───────┘
       │
       │ 1. Trigger Action (e.g., Test Alarm)
       ▼
┌──────────────────────┐
│ deviceActionsService │
└──────┬───────────────┘
       │
       │ 2. Check Device Online
       ▼
┌──────────────────┐
│      RTDB        │
│ devices/{id}/    │
│    state/        │
└──────┬───────────┘
       │
       │ 3. Write Action Request
       ▼
┌──────────────────┐
│      RTDB        │
│ devices/{id}/    │
│   actions/       │
└──────┬───────────┘
       │
       │ 4. Device Firmware Monitors
       ▼
┌──────────────────┐
│  Device Hardware │
│  (ESP8266/32)    │
└──────┬───────────┘
       │
       │ 5. Execute Action
       ▼
┌──────────────────┐
│  Update Status   │
│  (completed/     │
│   failed)        │
└──────────────────┘
```

### Critical Event Notifications Flow

```
┌──────────────────┐
│  Event Trigger   │
│ (Dose Missed,    │
│  Device Offline) │
└──────┬───────────┘
       │
       │ 1. Create Critical Event
       ▼
┌────────────────────────────┐
│ criticalEventNotifications │
│         Service            │
└──────┬─────────────────────┘
       │
       │ 2. Write to Firestore
       ▼
┌──────────────────┐
│   Firestore      │
│ criticalEvents/  │
└──────┬───────────┘
       │
       │ 3. Real-time Listener
       ▼
┌──────────────────┐
│  Caregiver App   │
│  (onSnapshot)    │
└──────┬───────────┘
       │
       │ 4. Send Push Notification
       ▼
┌──────────────────┐
│ Expo Notifications│
└──────┬───────────┘
       │
       │ 5. Display in UI
       ▼
┌──────────────────┐
│ CriticalEvents   │
│     Panel        │
└──────────────────┘
```

## Component Hierarchy

```
CaregiverDashboard
├── DeviceActionsPanel
│   ├── ActionCard (Test Alarm)
│   ├── ActionCard (Dispense Dose)
│   ├── ActionCard (Sync Time)
│   ├── ActionCard (Check Status)
│   └── ActionCard (Clear Alarm)
│
└── CriticalEventsPanel
    ├── EventCard (Dose Missed)
    ├── EventCard (Device Offline)
    ├── EventCard (Low Battery)
    └── EventCard (...)
```

## State Management

```
┌─────────────────────────────────────────────────────────┐
│              useCaregiverTreatmentControl               │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  Device Actions State:                                   │
│  ├── isActionInProgress: boolean                        │
│  ├── triggerTestAlarm: () => Promise<Result>           │
│  ├── dispenseManualDose: () => Promise<Result>         │
│  ├── syncDeviceTime: () => Promise<Result>             │
│  ├── checkDeviceStatus: () => Promise<Result>          │
│  └── clearAlarm: () => Promise<Result>                 │
│                                                           │
│  Critical Events State:                                  │
│  ├── criticalEvents: CriticalEventNotification[]       │
│  ├── unreadCount: number                                │
│  └── markEventAsRead: (id) => Promise<void>            │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## UI Components

### Device Actions Panel

```
┌────────────────────────────────────────────────────┐
│  ⚙️  Controles del Dispositivo                     │
│  Paciente: John Doe                                │
├────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────┐  ┌──────────────┐               │
│  │   🔔         │  │   💊         │               │
│  │ Probar       │  │ Dispensar    │               │
│  │ Alarma       │  │ Dosis        │               │
│  └──────────────┘  └──────────────┘               │
│                                                      │
│  ┌──────────────┐  ┌──────────────┐               │
│  │   ⏰         │  │   📊         │               │
│  │ Sincronizar  │  │ Verificar    │               │
│  │ Hora         │  │ Estado       │               │
│  └──────────────┘  └──────────────┘               │
│                                                      │
│  ┌──────────────┐                                   │
│  │   🔇         │                                   │
│  │ Silenciar    │                                   │
│  │ Alarma       │                                   │
│  └──────────────┘                                   │
│                                                      │
└────────────────────────────────────────────────────┘
```

### Critical Events Panel

```
┌────────────────────────────────────────────────────┐
│  🚨 Eventos Críticos                    [5]        │
├────────────────────────────────────────────────────┤
│                                                      │
│  ┌──────────────────────────────────────────────┐ │
│  │ 🔴  John Doe                          •      │ │
│  │     Patient missed their 8:00 AM dose        │ │
│  │     Medicamento: Aspirin                     │ │
│  │     hace 5 minutos                      →    │ │
│  └──────────────────────────────────────────────┘ │
│                                                      │
│  ┌──────────────────────────────────────────────┐ │
│  │ 📵  Jane Smith                               │ │
│  │     Device went offline                      │ │
│  │     hace 15 minutos                     →    │ │
│  └──────────────────────────────────────────────┘ │
│                                                      │
│  ┌──────────────────────────────────────────────┐ │
│  │ 🔋  Bob Johnson                              │ │
│  │     Device battery is low (15%)              │ │
│  │     hace 1 hora                         →    │ │
│  └──────────────────────────────────────────────┘ │
│                                                      │
└────────────────────────────────────────────────────┘
```

## Event Type Icons and Colors

| Event Type          | Icon | Color      | Background  |
|---------------------|------|------------|-------------|
| Dose Missed         | 🔴   | Orange 600 | Orange 50   |
| Device Offline      | 📵   | Red 600    | Red 50      |
| Low Battery         | 🔋   | Yellow 600 | Yellow 50   |
| Medication Deleted  | 🗑️   | Red 600    | Red 50      |
| Inventory Low       | 📦   | Orange 600 | Orange 50   |
| Device Error        | ❌   | Red 600    | Red 50      |

## Action States

### Device Action States

```
┌─────────────┐
│   Idle      │
└──────┬──────┘
       │
       │ User Presses Action
       ▼
┌─────────────┐
│  Loading    │ ← Shows spinner
└──────┬──────┘
       │
       │ Action Triggered
       ▼
┌─────────────┐
│  Pending    │ ← Waiting for device
└──────┬──────┘
       │
       ├─────────────┬─────────────┐
       │             │             │
       ▼             ▼             ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│  Success    │ │   Failed    │ │  Timeout    │
└─────────────┘ └─────────────┘ └─────────────┘
```

### Critical Event States

```
┌─────────────┐
│   Created   │
└──────┬──────┘
       │
       │ Firestore Write
       ▼
┌─────────────┐
│   Pending   │ ← notificationSent: false
└──────┬──────┘
       │
       │ Push Notification Sent
       ▼
┌─────────────┐
│  Notified   │ ← notificationSent: true
└──────┬──────┘      read: false
       │
       │ User Views Event
       ▼
┌─────────────┐
│    Read     │ ← read: true
└─────────────┘
```

## Permission Flow

```
┌──────────────┐
│  Caregiver   │
│   Attempts   │
│   Action     │
└──────┬───────┘
       │
       │ 1. Check Authentication
       ▼
┌──────────────┐
│  Firebase    │
│    Auth      │
└──────┬───────┘
       │
       │ 2. Check Device Link
       ▼
┌──────────────┐
│  Firestore   │
│ deviceLinks/ │
└──────┬───────┘
       │
       │ 3. Validate Permission
       ▼
┌──────────────┐
│  Security    │
│    Rules     │
└──────┬───────┘
       │
       ├─────────────┬─────────────┐
       │             │             │
       ▼             ▼             ▼
┌─────────────┐ ┌─────────────┐ ┌─────────────┐
│   Allow     │ │    Deny     │ │   Error     │
└─────────────┘ └─────────────┘ └─────────────┘
```

## Database Schema

### Firestore Collections

```
firestore/
├── users/
│   ├── {patientId}
│   │   ├── id: string
│   │   ├── role: "patient"
│   │   ├── deviceId: string
│   │   └── ...
│   └── {caregiverId}
│       ├── id: string
│       ├── role: "caregiver"
│       └── ...
│
├── devices/
│   └── {deviceId}
│       ├── id: string
│       ├── primaryPatientId: string
│       ├── provisioningStatus: string
│       ├── desiredConfig: object
│       └── ...
│
├── deviceLinks/
│   ├── {deviceId}_{patientId}
│   │   ├── deviceId: string
│   │   ├── userId: string
│   │   ├── role: "patient"
│   │   └── status: "active"
│   └── {deviceId}_{caregiverId}
│       ├── deviceId: string
│       ├── userId: string
│       ├── role: "caregiver"
│       └── status: "active"
│
├── medications/
│   └── {medicationId}
│       ├── name: string
│       ├── patientId: string
│       ├── caregiverId: string
│       ├── doseValue: string
│       ├── doseUnit: string
│       └── ...
│
├── medicationEvents/
│   └── {eventId}
│       ├── eventType: string
│       ├── medicationId: string
│       ├── patientId: string
│       ├── caregiverId: string
│       ├── timestamp: Timestamp
│       └── ...
│
└── criticalEvents/
    └── {eventId}
        ├── eventType: string
        ├── patientId: string
        ├── caregiverId: string
        ├── message: string
        ├── severity: string
        ├── read: boolean
        └── notificationSent: boolean
```

### RTDB Structure

```
rtdb/
├── devices/
│   └── {deviceId}/
│       ├── state/
│       │   ├── is_online: boolean
│       │   ├── battery_level: number
│       │   ├── current_status: string
│       │   ├── last_seen: number
│       │   └── time_synced: boolean
│       │
│       ├── config/
│       │   ├── alarm_mode: string
│       │   ├── led_intensity: number
│       │   ├── led_color: string
│       │   └── volume: number
│       │
│       └── actions/
│           └── {actionId}/
│               ├── actionType: string
│               ├── requestedBy: string
│               ├── requestedAt: number
│               ├── status: string
│               ├── completedAt?: number
│               └── error?: string
│
└── users/
    └── {userId}/
        └── devices/
            └── {deviceId}: true
```

## Integration Example

```typescript
// Complete integration example
import React from 'react';
import { View, ScrollView } from 'react-native';
import { useCaregiverTreatmentControl } from '../hooks/useCaregiverTreatmentControl';
import { DeviceActionsPanel } from '../components/caregiver/DeviceActionsPanel';
import { CriticalEventsPanel } from '../components/caregiver/CriticalEventsPanel';
import { useRouter } from 'expo-router';

export default function CaregiverDashboard() {
  const router = useRouter();
  const deviceId = 'DEVICE-001';
  const patientName = 'John Doe';

  const {
    criticalEvents,
    unreadCount,
    markEventAsRead,
  } = useCaregiverTreatmentControl(deviceId);

  const handleEventPress = async (event) => {
    await markEventAsRead(event.id);
    router.push(`/caregiver/events/${event.id}`);
  };

  return (
    <ScrollView>
      <View style={{ padding: 16 }}>
        {/* Device Actions */}
        <DeviceActionsPanel
          deviceId={deviceId}
          patientName={patientName}
        />

        {/* Critical Events */}
        <CriticalEventsPanel
          onEventPress={handleEventPress}
          maxEvents={5}
        />
      </View>
    </ScrollView>
  );
}
```

## Testing Flow

```
┌─────────────────┐
│  Run Test Suite │
└────────┬────────┘
         │
         │ 1. Setup Test Data
         ▼
┌─────────────────┐
│  Create Users   │
│  Create Device  │
│  Create Links   │
└────────┬────────┘
         │
         │ 2. Test Medication CRUD
         ▼
┌─────────────────┐
│  Create Med     │
│  Read Med       │
│  Update Med     │
│  Delete Med     │
└────────┬────────┘
         │
         │ 3. Test Device Actions
         ▼
┌─────────────────┐
│  Trigger Action │
│  Check Status   │
└────────┬────────┘
         │
         │ 4. Test Critical Events
         ▼
┌─────────────────┐
│  Create Event   │
│  Read Event     │
│  Mark as Read   │
│  Query Unread   │
└────────┬────────┘
         │
         │ 5. Cleanup
         ▼
┌─────────────────┐
│  Delete Test    │
│  Data           │
└────────┬────────┘
         │
         │ 6. Report Results
         ▼
┌─────────────────┐
│  ✅ 10/10 Pass  │
└─────────────────┘
```

## Related Documentation

- [Task 15 Completion Summary](./TASK15_COMPLETION_SUMMARY.md)
- [Quick Reference Guide](./CAREGIVER_TREATMENT_CONTROL_QUICK_REFERENCE.md)
- [Requirements Document](./requirements.md)
- [Design Document](./design.md)
